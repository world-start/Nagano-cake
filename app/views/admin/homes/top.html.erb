<h4>注文履歴一覧</h4>
<!--書き直す？-->

<table>
  <thead>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <!--belongs_toでモデルをつなぐ-->
      <% @orders.each do |order| %>
        <td><%= order.created_at %></td>
        <td><%= order.name %></td>
        <td><%= order.order_item.amount%></td>
        <td><%= order.order_status %></td>
      <% end %>
    </tr>
  </tbody>
</table>