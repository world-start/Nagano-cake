<h3>本当に退会しますか？</h3>

<p>
  退会すると、会員登録情報や<br>
  これまでの購入履歴が閲覧できなくなります。<br>
  退会する場合は、「退会する」をクリックしてください。
</p>

<%= form_with model: @customer, url: customer_renew_path(@customer), local:true do |f| %>
  <%= link_to '退会しない', customer_path, class: 'btn btn-primary' %>
  <%= f.submit '退会する', class: 'btn btn-danger' %>
  <%#= link_to '退会する', customer_renew_path,method:patch, class: 'btn btn-primary' %>
<% end %>